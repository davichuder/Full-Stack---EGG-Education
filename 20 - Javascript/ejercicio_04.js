// Algoritmo para verificar si un usuario ingresó 'S' o 'N' en JavaScript

// 1. Solicitar la entrada del usuario
var entrada = prompt("Introduzca 'S' o 'N': ");

// 2. Verificar si la entrada es 'S' o 'N'
if (entrada === 'S' || entrada === 'N') {
  console.log("CORRECTO");
} else {
  console.log("INCORRECTO");
}
